{
  "name": "treelio",
  "version": "0.1.0",
  "private": true,
  "description": "Treelio — 个人 AI 私人电台",
  "type": "module",
  "scripts": {
    "dev": "concurrently \"node server/index.js\" \"cd web && npx vite --port 5173\"",
    "build": "cd web && npx vite build --outDir ../server/public",
    "start": "NODE_ENV=production node server/index.js"
  },
  "dependencies": {
    "express": "^4.21.0",
    "node-fetch": "^3.3.2",
    "sql.js": "^1.11.0"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
